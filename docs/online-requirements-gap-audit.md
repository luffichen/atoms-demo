# 线上版本需求缺口审计（需求 01–102）

> 审计日期：2026-07-20（Asia/Shanghai）  
> 线上地址：`https://34.81.124.243.sslip.io`  
> 审计范围：`docs/requirements/01-*.md` 至 `102-*.md`，逐份阅读、逐项核对。编号 46 没有独立需求文档；需求 54 已被需求 99 和 TD-026 明确取消。  
> 审计方式：仅做只读线上验证，不在生产环境执行发布、放弃、并发冲突等会改变数据的操作；这些项目使用线上已有记录、已部署前端产物、服务端实现和自动化测试交叉核对。

## 结论

本轮共核对 101 份有效需求文档：

| 结论 | 数量 | 含义 |
| --- | ---: | --- |
| 已实现 | 100 | 当前线上行为或线上同版本实现有充分证据满足主要验收标准 |
| 部分实现 | 0 | 本轮发现的基础体验缺口已全部补齐并有回归测试 |
| 待专项验收 | 0 | 实时链路专项采样已完成并归档 |
| 已取消 | 1 | 需求 54 已被后续决策取消，不应再实现 |

因此，除已明确取消的需求 54 外，**100 项有效需求均已实现并完成验证**；本审计不再有部分实现或待专项验收项。

## 验证证据

- 线上健康检查返回 `200 {"ok":true}`。
- 线上首页加载前端产物 `/assets/index-CZX1TcrC.js`；对该产物中的页面、工作流和版本查看器逻辑与当前源代码进行了交叉核对。
- 线上默认访客、项目列表和项目详情 API 可读；抽查到：
  - 直接编码项目已有正式版本 V2、真实对话/命令/Todo/排队与取消记录。
  - 结构化需求项目 R001 已运行需求访谈、需求文档、技术方案、开发、测试准入、自动修复和测试报告流程。
- 本地 `npm run typecheck` 与 `npm run build` 通过。
- 依赖 ABI 已修复；本地完整测试现为 **32 个测试文件、223 项全部通过**。`npm run typecheck` 与 `npm run build` 也通过；项目未配置 Lint 脚本，发布门禁会将其明确记录为 `not_applicable`。
- `docs/realtime-latency-verification.md` 归档消息状态、用户可见文字、文件、终端与 Todo 的 WebSocket 和页面可见延迟：各类 50 个分层样本均在 1 秒内，页面可见 p95 为 3.0–10.1ms。

状态说明：

- ✅ 已实现：当前证据足以通过本轮验收。
- 🟡 部分实现：主体存在，但仍有明确缺口。
- 🔬 待专项验收：需要采样或压测，不能只靠代码判断。
- ⛔ 已取消：后续需求或技术决策已废止。

## 建议实施顺序

### P0：先补数据安全与发布完整性

1. **85（已完成）**：已统一阶段文件策略；开发、测试阶段冻结已确认需求/技术文档，Linux 终端沙箱同步只读挂载，待发布阶段冻结候选工作区，退回阶段后仅恢复对应文档写入。
2. **88、90（已完成）**：已统一执行测试、类型检查、Lint、构建、敏感内容和预览门禁；无脚本/非网页项目记录明确例外，发布中途失败会补偿回滚，确认框可编辑标题与摘要。
3. **97（已完成）**：所有工作流动作携带 `revision` 与幂等键；重复确认和过期状态被拒绝并刷新权威快照。重启、失败、超时和停止后暂停普通队列，只有人工继续或受控测试准入修复可恢复。
4. **99（已完成）**：图片已改为逐文件预上传，前端最多 3 个并发且不限制总张数，逐项展示进度/失败/重试/移除；消息只提交全部成功的上传 ID，主动移除立即删除，未发送上传 24 小时自动清理，正式附件长期保留并将需求/测试阶段图片归档到版本工作区资产目录。
5. **100（已完成）**：版本收集强制排除规则不再能被项目 `.gitignore` 覆盖，并拒绝 `.gitmodules`、gitlink、嵌套 Git、越界/失效链接、特殊文件及任意扩展名的超限二进制；发布前重复检查。

### P1：补齐版本工作流的可用闭环

1. **78（已完成）**：工作模式会在当前浏览器记忆，按钮与“新建需求：…”自然语言创建均在二次确认后才落地。
2. **91–96（已完成）**：已补齐放弃原因与只读快照、版本/工作记录元数据、当前工作详情、历史版本浏览、深链接与预览联动。
3. **98（已完成）**：追加式审计覆盖关键动作，简化时间线和站内通知均带版本/工作项深链。
4. **77、89（已完成）**：活动工作标题可修改且归档锁定；变更标记改为相对正式版本基线累计计算。

### P2：补基础体验与可观测性

1. **01、14–15、30、32、35、39、41、43–44、47、51、65–66（已完成）**：通用中文命名、稳定缩略图、事件时间、截断提示、条目分页、文件树语义、尺寸约束和无刷新重连均已补齐并回归。
2. **63、68（已完成）**：已建立五类实时事件的 WebSocket 与页面可见自动采样，并归档可重复报告。
3. **101（已完成）**：已完成 390/768/1024/1440 固定视口、44px 触控、版本 API p95 与发布中断恢复专项验收。

## 逐项核对结果

| 编号 | 状态 | 当前证据与缺口 / 下一步 |
| ---: | :---: | --- |
| 01 | ✅ | 自动命名支持中文、中英混合、常见英文主题及通用英文领域/产品组合；未命中特定主题仍生成中文兜底名，始终不超过 20 个字符且不直接复制完整需求。 |
| 02 | ✅ | 初始需求空白校验、错误提示和阻止创建均已实现。 |
| 03 | ✅ | 首页和项目内均支持选择/粘贴 PNG、JPEG 图片。 |
| 04 | ✅ | 已校验 PNG/JPEG 与单图 10MB 限制；错误不会清空仍有效的待发送内容。 |
| 05 | ✅ | 待发送缩略图、单项移除和发送后清空已实现。 |
| 06 | ✅ | 发送失败会保留文字与图片；每张图片拥有独立上传状态、错误说明和重试入口，只重试失败项，全部成功后才允许发送。 |
| 07 | ✅ | 默认访客存在，线上 API 可读到固定默认访客。 |
| 08 | ✅ | 自定义访客创建、名称校验和重名处理已实现。 |
| 09 | ✅ | 访客选择、记忆、异常回退和选择页错误态已实现。 |
| 10 | ✅ | 项目外可切换访客，切换后刷新项目上下文。 |
| 11 | ✅ | 项目内锁定访客，没有切换入口。 |
| 12 | ✅ | 访客状态使用 `sessionStorage`，不同标签页相互隔离。 |
| 13 | ✅ | 当前访客项目按服务端稳定顺序分页，每页 20 条；线上数据可验证。 |
| 14 | ✅ | 项目分页加载失败保留现有卡片并显示明确“重试加载”入口，重试从原游标继续。 |
| 15 | ✅ | 项目卡片展示持久化的最后一次成功预览缩略图；无缩略图时使用稳定占位，不运行活动 iframe。 |
| 16 | ✅ | 无项目时的空状态与创建入口已实现。 |
| 17 | ✅ | 点击项目卡片进入对应项目。 |
| 18 | ✅ | 不存在、无权限和加载失败均有独立错误处理。 |
| 19 | ✅ | 首页产品介绍和示例引导已实现。 |
| 20 | ✅ | 首页发送有效需求可创建项目并进入项目页。 |
| 21 | ✅ | 首页、项目列表等主页面导航已实现。 |
| 22 | ✅ | 桌面侧栏可折叠并保留核心导航。 |
| 23 | ✅ | 移动端抽屉导航与遮罩关闭已实现。 |
| 24 | ✅ | 追问空白校验和发送阻止已实现。 |
| 25 | ✅ | 运行中追问进入队列，线上已有排队记录。 |
| 26 | ✅ | 待处理消息可取消，线上已有取消记录。 |
| 27 | ✅ | 运行消息可停止，服务端终止与状态收敛已实现。 |
| 28 | ✅ | 失败、超时、重试/继续路径已实现。 |
| 29 | ✅ | 用户、Agent、系统内容与 Markdown 展示已实现。 |
| 30 | ✅ | 操作事件卡片展示状态、来源、原因与本地化事件时间。 |
| 31 | ✅ | 文件写入事件具备流式状态、摘要和终态；相关测试通过。 |
| 32 | ✅ | 终端输出保持 5MB 服务端边界，并持久化 `outputTruncated`、在事件 UI 明示“输出已截断”。 |
| 33 | ✅ | 对话自动跟随、用户主动上滚后停止跟随已实现。 |
| 34 | ✅ | 查看器输出跟随与用户滚动保护已实现。 |
| 35 | ✅ | 历史 API 按 conversation item 而非 turn 分页，每页最多 50 条；游标包含 turn 序号与 item 序号，同一 turn 内跨页顺序稳定且无重复，前端保留阅读位置和失败重试。 |
| 36 | ✅ | 应用、代码、终端、版本查看器切换已实现。 |
| 37 | ✅ | 根据运行命令、预览能力和文件状态选择默认查看器。 |
| 38 | ✅ | 应用预览支持手动刷新。 |
| 39 | ✅ | 预览失败保留错误与重试；成功加载会更新持久化缩略图，后续失败和项目列表均回退到最后成功画面。 |
| 40 | ✅ | 预览内路径导航和地址输入已实现。 |
| 41 | ✅ | 文件树仅按当前选择路径展开目录；初次进入自动选择根 README（存在时），目录可独立展开/收起。 |
| 42 | ✅ | 文本、图片、二进制/过大文件和语法高亮查看已实现。 |
| 43 | ✅ | Git 结构差异生成专用 rename/move 事件并携带旧、新路径；移动后的文件延续原有新增/修改身份。 |
| 44 | ✅ | 删除事件到达即乐观移除树节点并显示删除态，后续文件树快照再与服务端权威状态收敛。 |
| 45 | ✅ | 终端会话列表、输出与状态展示已实现。 |
| 47 | ✅ | 桌面拖拽比例同时受对话区 320px、工作区 480px 实际最小宽度约束，并对持久化比例做容器宽度校正。 |
| 48 | ✅ | 移动端对话/工作台表面切换已实现。 |
| 49 | ✅ | 项目对话区可折叠与恢复。 |
| 50 | ✅ | 离开运行中项目有确认，后台继续运行。 |
| 51 | ✅ | 断线自动重连和超时后的手动重连均原地重建实时连接并刷新权威数据，不整页刷新，当前草稿保持不变。 |
| 52 | ✅ | 服务重启后的运行状态恢复和队列继续机制已实现。 |
| 53 | ✅ | 可按项目能力启用或隐藏预览。 |
| 54 | ⛔ | 已被需求 99 与 TD-026 取消：不再设置项目总存储上限。 |
| 55 | ✅ | Agent 正常结束时会生成面向用户的完成消息。 |
| 56 | ✅ | 对歧义请求的假设说明/澄清约束已写入运行协议并有真实对话证据。 |
| 57 | ✅ | 演示数据持久保留，不因服务启动自动清理。 |
| 58 | ✅ | 同一项目多个标签页通过实时事件同步。 |
| 59 | ✅ | 草稿按项目和标签页隔离管理。 |
| 60 | ✅ | 已发送图片可在对话中预览。 |
| 61 | ✅ | 后台任务完成/失败通知与回到项目的入口已实现。 |
| 62 | ✅ | 预览进程启动、复用、停止和回收逻辑已实现并有测试。 |
| 63 | ✅ | Agent 用户可见文字按同一 item 原位增量追加，重连用权威快照补齐且不重复；30 个 WebSocket 与 20 个页面可见分片样本全部小于 1 秒，页面 p95 3.2ms。 |
| 64 | ✅ | 对话文本安全渲染、链接和代码块处理已实现。 |
| 65 | ✅ | 新建、修改、删除、rename/move 均实时更新文件树；当前选中文件移动后切换到新路径并继续展示其内容。 |
| 66 | ✅ | 项目、消息及操作事件均通过统一本地时间组件展示，覆盖所有用户可见时间信息。 |
| 67 | ✅ | 用户、Agent 和产品身份标识已实现。 |
| 68 | ✅ | 消息状态、文字分片、文件写入、终端输出和 Todo 分别完成 30 个真实 WebSocket 与 20 个页面可见样本，100% 小于 1 秒，页面 p95 3.0–10.1ms；详见 `docs/realtime-latency-verification.md`。 |
| 69 | ✅ | 实施记录中线上 API p95 为 247.6ms，页面有骨架/加载态，满足本轮证据要求。 |
| 70 | ✅ | 项目卡片在 768–1023px 为 2 列、1024px 以上为 3 列，并限制最大宽度。 |
| 71 | ✅ | 预览 iframe 使用 sandbox 且不授予 `allow-same-origin`，并配置 CSP 隔离。 |
| 72 | ✅ | 当前用户消息显示本人头像，其他消息身份区分清楚；测试覆盖。 |
| 73 | ✅ | Agent 思考/工作中状态及 `prefers-reduced-motion` 兼容已实现。 |
| 74 | ✅ | Agent 在工具调用前输出进度说明；协议、测试和线上对话均有证据。 |
| 75 | ✅ | 多步骤任务使用 Todo 计划、状态更新和进度展示；线上有真实 Todo 记录。 |
| 76 | ✅ | 每个项目独立 Git 仓库、`main` 基线和 V1 初始化已实现；线上版本数据可验证。 |
| 77 | ✅ | 工作项 ID、两类类型、稳定递增 R 编号、创建/归档时间和终态归档完整；活动工作可通过 revision 保护的 API/UI 修改标题，发布或放弃后标题锁定，后续工作创建新 ID。 |
| 78 | ✅ | 首页与项目空工作区提供直接编码/按需求研发选择并在浏览器记忆；“当前工作”区域也提供入口。“新建需求：…”会解析为自然语言来源，按钮和自然语言结构化创建都先二次确认，确认前不创建工作项、分支、对话或 session。 |
| 79 | ✅ | 工作项拥有独立对话和 session 路径，历史记录按工作项隔离。 |
| 80 | ✅ | 数据库唯一约束和 API 均限制一个活动工作项，分支从当前正式 `main` 创建。 |
| 81 | ✅ | 结构化需求访谈协议、只写需求文档限制及线上 R001 记录均已验证。 |
| 82 | ✅ | 需求讨论提示强制每个决策增量更新草稿、标记待确认项并维护冲突变更记录；正式确认时服务端校验 README 的目标/范围/非目标/依赖/原子索引及每个原子文档的规定结构，并拒绝待确认项。图片先放版本排除的暂存区，只有 Markdown 实际引用的图片会复制到对应需求包 `assets/` 并改写为相对路径，未引用图片只保留正式对话附件。 |
| 83 | ✅ | 工作流栏展示需求编号、阶段、执行状态和主要操作；退回、放弃等低频危险动作已收进“更多”菜单。按钮与自然语言共用同一确认/执行 API，运行与排队状态由前后端双重阻止流转，revision 与幂等键拒绝重复/并发动作。只读阶段事件卡已插入对话，展示系统/按钮/自然语言触发方式、流转结果和退回原因。 |
| 84 | ✅ | 确认需求后先创建正式需求整理轮次，需求包结构硬门禁通过并创建检查点后才自动进入技术设计。技术方案完整覆盖背景与需求引用、现状、方案、数据、接口事件、交互、安全、兼容、实施、测试、回滚、风险和非目标且无待确认项后，系统自动进入技术方案待确认；确认动作再次服务端复检、创建检查点并自动开始开发。 |
| 85 | ✅ | 已实现统一阶段写入策略：需求/技术阶段仅开放对应文档，开发、测试阶段冻结已确认需求与技术方案，Linux 终端使用同一只读边界；待发布阶段关闭终端并冻结候选工作区，退回阶段后仅恢复对应文档写入。文件工具、阶段策略与沙箱参数均有回归测试。 |
| 86 | ✅ | 开发在独立工作分支执行且只读已确认文档；进入测试准入前固定创建“实现完成”检查点。明确的需求/技术范围变更会复用服务端退回动作并二次确认，不会静默吸收到代码；停止、失败和重启保留现场，人工重试先读取工作区、基线 Diff、待办和检查点。 |
| 87 | ✅ | 测试准入通过后自动进入测试；提示强制运行已有测试、类型检查、Lint、构建和方案命令，自动修复后复检。进入待上线前服务端硬校验测试报告的环境、检查点、命令、耗时、摘要、逐项验收映射、界面尺寸、限制和通过结论；界面项目自动采集并引用 1440×900 桌面与 390×844 移动截图，校验每张不超过 10 MB，再创建“测试通过”检查点并冻结候选版本。 |
| 88 | ✅ | 已完成结构化需求原子发布：待上线预览显示 `Rxxx` 标识，发布前复检质量脚本、敏感内容和候选预览；发布记录包含来源、需求、基线、候选提交、tag、确认身份/时间、变化、文档、测试、预览和限制。Git 合并/tag/分支删除具备内部补偿，正式预览或数据库记录失败会恢复 main、tag 与工作分支；失败保持待上线且不占版本号。 |
| 89 | ✅ | 直接编码从当前正式基线创建独立分支，多轮成功且有变化才建立检查点，失败/停止保留现场且不伪造完成；文件树通过 Git 将已提交与未提交变化统一相对工作项 `baseCommit` 累计计算，新工作项使用独立 session。 |
| 90 | ✅ | 直接编码发布已接入统一门禁：执行项目已有的 test/typecheck/lint/build，扫描敏感内容并复检预览；缺少脚本或非网页项目会明确记录 `not_applicable`，空候选仍被拒绝。二次确认框提供可编辑标题和摘要，失败后关闭旧确认并要求重新确认；发布说明明确来源为直接编码且不伪造需求文档。 |
| 91 | ✅ | 放弃动作强制填写原因并保留时间；结构化工作在归档前创建内部快照提交、保留分支，历史记录可只读浏览文件树、相对基线 Diff、文档和元数据；直接编码仍按规则清除代码且不提供恢复。 |
| 92 | ✅ | “当前工作 / 正式版本 / 工作记录”三级视图、20 条分页和空态完整；正式版本按倒序展示当前标记、来源、需求、标题、时间与摘要，项目卡片同步显示当前 Vn、活动 R 阶段或未发布状态。 |
| 93 | ✅ | 正式版本详情展示来源、需求、状态、确认游客、tag、短 commit、基线、文档完整度、文件统计与新增/修改/删除/重命名列表；逐文件 Diff 按需读取并保留截断、二进制和只读边界。 |
| 94 | ✅ | 历史文件继续直接读取 Git tree/blob，不 checkout 工作区；查看器显示“正在查看 Vn”与“返回当前代码”，版本、详情模式和文件路径均写入可复制深链接并可刷新恢复。 |
| 95 | ✅ | 工作记录展示类型、需求、状态、关联版本、创建和结束时间；发布项可跳正式版本，归档对话/终端只读并提供返回当前工作/开始新工作；已放弃结构化工作展示原因、时间、文件树和基线 Diff。 |
| 96 | ✅ | 活动结构化需求重新打开默认进入“版本 → 当前工作”，直接编码未发布只显示徽标且不强制切换；预览按阶段显示“开发预览 · Rxxx”“待上线预览 · Rxxx”“正式版本 · Vn”或“开发预览 · 未发布”。 |
| 97 | ✅ | 工作流动作现强制携带服务端 `revision` 与持久化幂等键：同一确认最多成功一次，不同键的过期动作返回冲突，前端失败后刷新权威快照。多标签仍通过实时事件同步。服务重启只将执行中轮次收敛为失败并保留现场/队列，不再自动启动；失败、超时和停止后普通队列暂停，人工“继续执行”无需二次确认并创建高优先级恢复轮次，明确先检查工作区、基线 Diff、待办和最近检查点，无法判断时只问一个问题。发布期间持久化 `running` 进度，刷新后可继续观察最终收敛。 |
| 98 | ✅ | 追加式审计现覆盖创建、标题、阶段/退回原因与来源、二次确认、检查点、停止、重试、失败、发布尝试/失败/成功和放弃；当前对话与归档工作记录显示简化时间线。需求/技术待确认、开发完成、测试通过待上线、失败和发布成功生成去重站内通知，深链到当前工作或正式版本；版本详情同时展示发布发起与确认游客。 |
| 99 | ✅ | 已取消图片数量/总量限制并保留 PNG/JPEG 单图 10MB 校验；前端逐文件上传、最多 3 个并发，显示进度、失败、单项重试与移除，全部上传成功后仅提交附件 ID。服务端支持主动删除与 24 小时清理，消息消费后转为长期附件，结构化需求和测试阶段图片复制到对应编号的版本化文档资产目录；8 张图片发送、生命周期和归档均有回归测试。 |
| 100 | ✅ | 已完成版本内容边界：Git pathspec 强制排除平台元数据、依赖、缓存、产物、日志、运行状态与秘密，项目 `.gitignore` 无法反向包含；拒绝 `.gitmodules`、submodule gitlink、嵌套 `.git`、越界/失效符号链接、设备/Socket/FIFO 等特殊文件，并按内容识别任意扩展名的 10MB 以上二进制。发布前会重写排除规则并重复检查，历史 ref/path 继续使用白名单校验；8 项版本控制测试覆盖关键边界。 |
| 101 | ✅ | 当前工作提供概览、需求、技术方案、变更、测试、发布六页签及阶段锁定说明，版本化文档只读展示并支持页签/文件深链接。390/768 单列且交互项至少 44px，1024/1440 紧凑双栏，四档均无页面级横向溢出；Fastify+SQLite+Git 各 20 次采样 p95 为 0.1–9.9ms，发布进度刷新恢复有回归测试，详见 `docs/version-workflow-verification.md`。 |
| 102 | ✅ | 测试准入、命令报告解析、最多两轮自动修复和失败收敛已实现；线上 R001 已出现准入失败后重试的真实记录。 |

## 可直接拆分的实施批次

为避免一次改动跨越过多领域，建议按下面的批次逐个补齐并分别上线：

| 批次 | 需求 | 上线验收重点 |
| --- | --- | --- |
| A（已完成） | 85、100 | 已验证文档冻结、终端只读边界、恶意 Git/特殊文件/大二进制拒绝 |
| B（已完成） | 88、90、97 | 已验证发布预检、原子回滚、revision 冲突、幂等重复提交和人工恢复 |
| C（已完成） | 99、06 | 已验证 8 张图片发送、最多 3 个并发、逐项进度/失败/重试/移除、即时删除、24 小时清理、正式附件留存与文档资产归档 |
| D（已完成） | 82–84、86–87 | 已验证需求包/技术方案/测试报告结构门禁、阶段事件与原因、实现/测试检查点、桌面和移动截图及待上线冻结 |
| E（已完成） | 91–96、101 | 已验证当前工作/版本/工作记录完整查看、放弃快照、深链接、四档响应式、触控和性能 |
| F（已完成） | 77–78、89、98 | 已验证模式记忆与二次确认、标题终态锁定、正式基线累计标记、审计事件与深链通知 |
| G（已完成） | 01、14–15、30、32、35、39、41、43–44、47、51、65–66 | 已验证通用命名、稳定缩略图、条目分页、事件时间/截断、文件移动/删除、实际宽度与无刷新重连 |
| H（已完成） | 63、68 | 已完成五类实时内容的 WebSocket/页面可见自动采样并归档报告 |

每个批次上线前至少应执行：依赖 ABI 修复后的完整测试、`npm run typecheck`、`npm run build`、对应需求的新增回归测试、桌面 1440px 与移动 390px 冒烟，以及线上只读/可回滚验证。
